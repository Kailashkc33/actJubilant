{
  "name": "nextjs-cloud-agent",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "if [ ! -f node_modules/.package-lock.json ] || ! cmp -s package-lock.json node_modules/.package-lock.json; then npm ci; else echo 'Dependencies already in sync with package-lock.json'; fi"
}
